# holter-site environment variables
#
# No runtime env vars are required for local development today.
# Build-time secrets (e.g. pricing API key, Cloudflare token) will land here
# when the AIDA / Cloudflare Pages stages are wired up.
